<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0"/>
<title>Premium Access — Black & Gold</title>

<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700;800&display=swap" rel="stylesheet">

<style>
:root{
  --gold:#d4af37;
  --gold-soft:#ffdf8a;
  --dark:#000000;
  --dark-2:#121212;
  --card:#0b0b0b;
  --accent:#25D366;
  --text:#f5f5f5;
}

*{margin:0;padding:0;box-sizing:border-box;}
html,body{
  width:100%;height:100%;
  font-family:'Poppins',sans-serif;
  color:var(--text);
  overflow:hidden;
  cursor:pointer;
}

/* 🖤💛 BACKGROUND */
body{
  background: linear-gradient(120deg,var(--dark),var(--dark-2),var(--gold));
  background-size:300% 300%;
  animation:bgMove 14s ease infinite;
  display:flex;
  align-items:center;
  justify-content:center;
  text-align:center;
}
@keyframes bgMove{
  0%{background-position:0% 50%}
  50%{background-position:100% 50%}
  100%{background-position:0% 50%}
}

/* ✨ OBJECTS — glows, particles, streaks */
.glow{
  position:absolute;
  width:260px;height:260px;
  background:radial-gradient(circle,var(--gold),transparent 60%);
  filter:blur(48px);
  opacity:.35;
  animation:float 9s ease-in-out infinite;
}
.g1{top:-60px;left:-60px;}
.g2{bottom:-80px;right:-80px;animation-delay:2s;}
.g3{top:60%;left:60%;animation-delay:4s;}

@keyframes float{
  0%,100%{transform:translateY(0)}
  50%{transform:translateY(-30px)}
}

/* Golden particles */
.particle{
  position:absolute;
  width:6px;height:6px;border-radius:50%;
  background:var(--gold-soft);
  opacity:.7;
  animation:rise 6s linear infinite;
}
@keyframes rise{
  from{transform:translateY(20px);opacity:.0}
  to{transform:translateY(-120vh);opacity:.9}
}

/* Light streak */
.streak{
  position:absolute;
  width:120px;height:2px;
  background:linear-gradient(90deg,transparent,var(--gold-soft),transparent);
  opacity:.6;
  animation:streak 5s linear infinite;
}
@keyframes streak{
  from{transform:translateX(-120vw)}
  to{transform:translateX(120vw)}
}

/* CARD */
.card{
  z-index:3;
  width:92%;
  max-width:440px;
  padding:32px 26px 34px;
  border-radius:26px;
  background:linear-gradient(180deg,rgba(20,20,20,.75),rgba(10,10,10,.85));
  backdrop-filter:blur(16px);
  box-shadow:0 30px 80px rgba(0,0,0,.65);
  border:1px solid rgba(212,175,55,.35);
  animation:pop .9s ease;
}
@keyframes pop{
  from{transform:scale(.86);opacity:0}
  to{transform:scale(1);opacity:1}
}

/* BADGE */
.badge{
  display:inline-block;
  padding:7px 18px;
  border-radius:999px;
  background:linear-gradient(90deg,var(--gold),var(--gold-soft));
  color:#2b2300;
  font-weight:800;
  font-size:12px;
  letter-spacing:.4px;
  margin-bottom:14px;
}

/* HEADINGS */
h1{
  font-size:30px;
  line-height:1.15;
  margin-bottom:10px;
  color:var(--gold-soft);
}
.desc{
  font-size:15px;
  opacity:.95;
  margin-bottom:8px;
}

/* META-FRIENDLY DESCRIPTION */
.meta{
  font-size:13px;
  opacity:.85;
  margin-bottom:14px;
}

/* TIMER */
.timer{
  margin:18px auto 20px;
  width:132px;height:132px;border-radius:50%;
  display:grid;place-items:center;
  background:conic-gradient(var(--gold) calc(var(--p)*1%),rgba(255,255,255,.15) 0);
  animation:pulse 1.6s ease infinite;
}
@keyframes pulse{
  0%,100%{transform:scale(1)}
  50%{transform:scale(1.06)}
}
.inner{
  width:102px;height:102px;border-radius:50%;
  background:rgba(0,0,0,.75);
  border:1px solid rgba(212,175,55,.45);
  display:grid;place-items:center;
}
.count{
  font-size:42px;font-weight:800;color:var(--gold-soft);
}

/* CTA */
.cta{
  display:inline-block;
  padding:15px 30px;
  border-radius:999px;
  background:linear-gradient(90deg,#25D366,#7CFFB2);
  color:#043b2a;
  font-weight:900;
  text-decoration:none;
  box-shadow:0 14px 36px rgba(37,211,102,.55);
  animation:bounce 1.8s ease infinite;
}
@keyframes bounce{
  0%,100%{transform:translateY(0)}
  50%{transform:translateY(-6px)}
}

.note{
  font-size:12px;opacity:.8;margin-top:12px;
}
.tap{
  position:absolute;bottom:10px;
  font-size:11px;opacity:.65;
  animation:blink 1.6s infinite;
}
@keyframes blink{
  0%,100%{opacity:.35}
  50%{opacity:.85}
}
</style>
</head>

<body>

<!-- Objects -->
<div class="glow g1"></div>
<div class="glow g2"></div>
<div class="glow g3"></div>

<div class="streak" style="top:20%"></div>
<div class="streak" style="top:55%;animation-delay:1.5s"></div>

<!-- Particles (duplicate for density) -->
<div class="particle" style="left:10%;animation-delay:0s"></div>
<div class="particle" style="left:25%;animation-delay:1s"></div>
<div class="particle" style="left:40%;animation-delay:2s"></div>
<div class="particle" style="left:55%;animation-delay:3s"></div>
<div class="particle" style="left:70%;animation-delay:4s"></div>
<div class="particle" style="left:85%;animation-delay:5s"></div>

<div class="card">
  <div class="badge">PREMIUM • SECURE • DIRECT</div>
  <h1>Elite WhatsApp Access</h1>

  <div class="desc">
    Clean, professional interface designed for smooth navigation and quick access.
  </div>

  <div class="meta">
    This page provides a simple way to continue on WhatsApp for further information.
    No unnecessary steps. Optimized for mobile experience and fast loading.
  </div>

  <div class="timer" id="ring" style="--p:100">
    <div class="inner">
      <div class="count" id="count">5</div>
    </div>
  </div>

  <a href="#" class="cta" id="cta">Continue on WhatsApp</a>
  <div class="note">You will be redirected automatically…</div>
</div>

<div class="tap">Tap anywhere to proceed</div>

<script>
const WHATSAPP = "https://wa.me/8471088676";
let t = 5;
const count = document.getElementById("count");
const ring = document.getElementById("ring");

document.body.addEventListener("click",()=>{
  window.location.href = WHATSAPP;
},{ once:true });

document.getElementById("cta").onclick = (e)=>{
  e.preventDefault();
  window.location.href = WHATSAPP;
};

const iv = setInterval(()=>{
  t--;
  count.innerText = t;
  ring.style.setProperty("--p",(t/5)*100);
  if(t<=0){
    clearInterval(iv);
    window.location.href = WHATSAPP;
  }
},1000);
</script>

</body>
</html>
